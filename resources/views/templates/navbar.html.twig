<nav class="navbar navbar-expand-sm bg-dark navbar-dark">
  <a class="navbar-brand" href="/">{{ language.title }}</a>
  <ul class="navbar-nav">
    <li class="nav-item">
      <a class="nav-link" href="{{ path_for('selling') }}">{{ language.selling }}</a>
    </li>
    <li class="nav-item">
      <a class="nav-link" href="{{ path_for('buying') }}">{{ language.buying }}</a>
    </li>
  </ul>
  <a href="{{ path_for('new_listing') }}" class="btn btn-success ml-auto mr-2" role="button">{{ language.new }}</a>
</nav> 