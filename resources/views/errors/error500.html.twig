{% extends 'templates/base.html.twig' %}

{% block content %}
  <h1>{{ language.error500_title }}</h1>

  <p>
    {{ language.error500_text }} <a href="{{ path_for('homepage', { 'lang': language.code }) }}">{{ language.return_to_homepage }}</a>
  </p>
{% endblock %}