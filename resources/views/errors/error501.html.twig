{% extends 'templates/base.html.twig' %}

{% block content %}
  <h1>{{ language.error501_title }}</h1>

  <p>
    {{ language.error501_text }} <a href="{{ path_for('homepage') }}">{{ language.return_to_homepage }}</a>
  </p>
{% endblock %}