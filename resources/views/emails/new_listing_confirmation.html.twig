<h1>Hello</h1>

<p>Your new listing can be found here: <a href='{{ server_address }}{{ path_for('single_listing', { 'id': params.insertedId, 'lang': params.language.code }) }}'>{{ server_address }}{{ path_for('single_listing', { 'id': params.insertedId, 'lang': params.language.code }) }}</a></p>

<p>To remove it, use your removal code: {{ params.removalCode }}</p>

<p>Love, Admin</p>
